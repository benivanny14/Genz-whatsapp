Place `release.jks` here locally. Never commit the keystore or passwords.

Required environment variables for `npm run apk:build`:

- KEYSTORE_PATH (defaults to this folder's release.jks)
- KEYSTORE_PASSWORD
- KEY_ALIAS
- KEY_PASSWORD
