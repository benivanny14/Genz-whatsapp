import React from 'react'
import StatusList from '../components/StatusList'

/**
 * Status page - WhatsApp-style status/stories feature.
 * Uses StatusContext for data, StatusList for the feed UI,
 * StatusViewer for viewing, CreateStatus for posting, StatusPrivacy for settings.
 */
const Status = () => {
  return <StatusList />
}

export default Status
